requires:
  features:
    - HAVE_LIBJANSSON
  lt-version: 7

checks:
  - filter:
      count: 1
      match:
        dns.type: query
        dns.queries[0].rrname: suricon.net
  - filter:
      count: 1
      match:
        dns.type: response
        dns.rdata: "181.224.138.142"
